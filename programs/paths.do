/* define global paths */
global trunk ".."                                                        // CHANGE THIS FOR SPECIFIC SYSTEM
global inpath "${trunk}/data/input"                                      // location of input data sources
global wrkpath "${trunk}/data/interwrk"                                  // working directory 
global figpath "${trunk}/output/figures"
global tabpath "${trunk}/output/tables"
global programs "${trunk}/programs"    
global adobase  "${trunk}/programs/ado" 